# 발표 숫자 검증 정정표

`발표 대본.md` · `발표 계획.md` · `docs/분석_진행_정리.md` · `README.md` 에 적힌 숫자를
`outputs/` 산출물, `data/derived/` parquet, `src/` 코드와 직접 대조한 결과다.

- 검증일: 2026-08-03 (커밋 `33fb632` 기준)
- 범위: 70개 주장 검증, **15건에서 문제 발견**
- 방법: 8개 주제군을 독립 병렬 검증 후 교차 대조. 모든 값은 파일에서 직접 읽거나 parquet에서 재계산했다.

> **읽는 법** — 1장은 반드시 고쳐야 할 것, 2장은 검증 통과한 값, 3장은 같은 숫자가
> 여러 뜻으로 쓰이는 구간(질의응답 최대 위험), 4장은 예상 질문 답변, 5장은 미확인 항목.

---

경로 표기: 아래 모든 파일 경로는 `/Users/vista/Desktop/프로젝트/2026 SL/` 하위 기준입니다.

---

# 1. 즉시 고쳐야 할 것

## 1-1. 숫자가 산출물과 다른 것

| # | 계획서·문서 값 | 실제 값 | 출처 파일 | 어떻게 고칠지 |
|---|---|---|---|---|
| A | 슬라이드 19: 183.9→212.3(+28.4m) 옆에 **경사 +27.2m / 기상 +18.1m / 상호작용 +1.8m** | 27.2/18.1/1.8은 **hidden 632개** 평균(합 47.149m). 전체 4,383개 기준은 **경사 +15.42m / 기상 +11.96m / 상호작용 +1.00m** (합 28.386m) | 27.2/18.1/1.8: `outputs/reports/hidden_vulnerability_reason_diagnostics_qa.json` (`mean_slope_increment_m1_m0` 등) · 전체 기준 재계산: `data/derived/hex_vulnerability_final.parquet` (`access_cost_m0`~`m3`) · 정의: `src/sl_accessibility/accessibility/vulnerability.py:142-145` | 분해값을 **15.4 / 12.0 / 1.0 (합 28.4m)** 로 교체. 합이 헤드라인 28.4m와 정확히 맞고 "경사가 절반 이상(54.3%)" 메시지도 유지됨. 27.2/18.1/1.8을 쓰려면 슬라이드를 분리하고 "숨겨진 취약 632개 기준(274.8m→322.0m, +47.1m)"로 모집단을 명시 |
| B | "Jaccard 0.667 → **약 1/3 교체**" | 각 집합 교체율 **19.95%** (175/877). 33.27%는 1−Jaccard(자카드 거리)로, 합집합 중 비공유 비율이지 교체율이 아님 | `docs/분석_진행_정리.md:346` (오류 원문) · 반증: `outputs/reports/m4_senior/m4_senior_hex_long.csv` × `data/derived/hex_vulnerability_final.parquet` (교집합 702 / 합집합 1,052) | "**각 후보군의 약 20%가 교체 (Jaccard 0.667, 교집합 702개)**"로 교체. `발표 계획.md:141,184`에 이미 같은 정정안이 있음. 원본 `docs/분석_진행_정리.md:346`도 함께 수정하지 않으면 오류가 재유입됨 |
| C | 슬라이드 22 답변안: "순위가 같아도 **임계 근처 격자가 뒤집혀서 632개**가 나온다" | 반증됨. 거리만(M0)으로 취약도를 계산해도 hidden은 **634개**. 교집합 574, 신규 58, 탈락 60 → 경사·기상이 hidden 명단에 더한 순증은 **−2개** | `발표 계획.md:164` (주장) · 반증: `outputs/reports/v2_2_execution/m0_m3_environment_burden_effect.csv` (`official_400m_ok_m0` × `m0_frozen_vulnerable` × `hidden_vulnerable_final` 교차집계) | 이 답변을 대본에서 삭제. 4-1의 4층 답변으로 교체. 이대로 쓰면 "그럼 M0로도 632개 아니냐"는 재반문에 무너짐 |
| D | "632개는 현행 400m 기준으로 양호하지만 **경사·기상을 반영하면 취약으로 바뀐다**" | 632개 중 **574개(90.8%)는 거리만으로도 이미 취약**. 경사·기상 때문에 새로 취약해진 것은 **58개(9.2%)** | `README.md:50`, `docs/분석_진행_정리.md:311` (주장) · 반증: `outputs/reports/v2_2_execution/m0_m3_environment_burden_effect.csv` (`hidden_vulnerable_final` × `m0_frozen_vulnerable`) | "경사·기상을 반영하면" → "**수요까지 함께 보면**"으로 교체. 경사·기상의 기여는 별도로 "신규 취약 275개"로 말할 것 |
| E | "4,383개 중 877개 = **19.3%**, hidden 632 = **13.9%**" | 877/4,383 = **20.0%**, 632/4,383 = **14.4%**. 19.3%·13.9%는 분모가 4,551일 때 값 | `docs/분석_진행_정리.md:300,304,311` (주장) · `outputs/reports/v2_2_execution/m0_m3_environment_burden_effect.json` (`valid_hex_rows`=4383) | 분모를 4,383으로 통일하고 **20.0% / 14.4%** 로 수정. 문장 내부 모순이라 그대로 두면 계산기로 즉시 걸림 |
| F | 슬라이드 23: 191+167+140 = 498 (**78.8%만 설명, 나머지 134개는 이름 없음**) | 나머지 134개도 이미 이름이 있음: **혼합형 86 / 400m 경계형 35 / 수요+경계형 13**. 6분류가 632를 정확히 채움(191+167+140+86+35+13=632) | `outputs/reports/hidden_vulnerability_reason_diagnostics_qa.json` → `primary_reason_counts` (6개 키) · 한글 라벨 `scripts/build_midterm_visuals.py:42-49` | 4번째 항목 "**혼합·경계 134개(21.2%)**" 한 줄 추가하면 즉시 100%. 새 분석 불필요. 기성 그림 `outputs/figures/midterm/fig07_reason_diagnostics.png`(6범주)나 `outputs/qgis_midterm_maps/S8_reason_diagnostics.png`(4범주 접기)를 그대로 써도 됨 |
| G | `docs/분석_진행_정리.md`: "**복합 원인 86 — 여러 요인 혼합**" | 코드상 `mixed`는 세 압력(수요·환경·거리)이 **모두 없는** 잔여 범주. CSV 86행 전부 세 플래그 False | `docs/분석_진행_정리.md:325` (오류) vs `src/sl_accessibility/accessibility/vulnerability.py:174` · `outputs/reports/hidden_vulnerability_reason_diagnostics.csv` | "여러 요인 혼합" → "**뚜렷한 단일 원인 없음(잔여군)**". 발표에서는 86을 35·13과 묶어 "혼합·경계 134"로 처리하면 이 질문 자체가 사라짐 |

## 1-2. 근거 파일·인용을 바꿔야 할 것

| # | 잘못된 인용 | 올바른 근거 | 출처 |
|---|---|---|---|
| H | 원인 3분류의 근거가 `typology.py` | `typology.py`는 별개의 **4유형 분류기**(supply_shortage/slope_vulnerable/weather_amplified/high_demand)이고 산출물이 `outputs/`에 저장돼 있지도 않음. 실제 근거는 `build_hidden_vulnerability_diagnostics` | `src/sl_accessibility/accessibility/vulnerability.py:111-195` (실제 근거) vs `src/sl_accessibility/accessibility/typology.py:25-124`. 후자 독스트링 14-17행이 임계값을 "초기 탐색값, 확정값 아님"이라고 자기 명시 → **질의응답에서 인용 금지** |
| I | 슬라이드 22 강건성에 `hidden_environment_burden_admin_summary.csv`의 `normalization_sensitive_count` 사용 | 이 열의 합은 **203**(행정동 라벨이 붙은 632행만 집계된 값). 정확한 값 **624**는 다른 파일에 있음 | 624: `outputs/reports/v2_2_execution/hidden_vulnerable_robust_core_summary.json` → `normalization_sensitive_rows`. 203의 원인: `scripts/build_v2_2_execution_artifacts.py:245` dropna로 421행 탈락 |
| J | 계단 edge **5,632개** | 실행값은 **5,628개** (4개 차이) | 문서: `outputs/reports/plan_review_v2/05_m4_senior_impedance_method.md:110` / 실행값: `outputs/reports/m4_senior/m4_senior_summary.json` → `steps_edge_count`. **실행값을 인용할 것** |
| K | 슬라이드에 "877 = 4,383 × 20%" 곱셈식 | 4,383×0.2 = 876.6이라 산술이 안 맞음. 실제는 80분위 임계(0.0288386) **이상(>=)** 카운트 = 877 (20.009%) | `data/derived/hex_vulnerability_final.parquet` 재계산 · `src/sl_accessibility/accessibility/vulnerability.py:79-81`. "상위 20% 분위 임계 이상 = 877개"로 표기 |

## 1-3. 숫자는 맞지만 표현이 과한 것 (안전 표현으로 교체)

| 현재 표현 | 안전한 표현 | 근거 |
|---|---|---|
| "hex당 평균 15.4% 증가" / "평균적인 격자가 15.4% 올랐다" | "**서울 전체 평균 접근비용이 15.4% 증가**" | 15.4%는 평균의 비율(ratio of means). 격자별 증가율의 단순평균은 15.16%, 중앙값은 11.19% (`outputs/reports/hex_vulnerability_summary_stats_v3.csv` 행 `cost_gap_ratio_m3_over_m0`, n=3,933) |
| "고령자가 훨씬 더 큰 부담을 진다" | "**고령인구 가중 증가율이 비가중보다 높다(15.84% > 15.67% > 15.44%)**" | 차이가 0.4%p로 작음. `m0_m3_environment_burden_effect.json` |
| "hidden 632개는 고령 밀집지역" | "**고령 비중이 서울 평균 수준 이상(20.60% vs 19.97%)**" | 차이 +0.63%p. `outputs/reports/hex_vulnerability_final_qa.json` |
| "166만 명이 피해를 본다 / 격자 안에 166만 명이 산다" | "**후보 격자에 걸린 등록인구 약 166만 명**" | 행정동→H3 면적가중 배분 추정치(소수점 존재), 헤드카운트 아님 |
| "S1으로 49개가 해결된다" | "**후보 48개를 모두 동시에 반영한 네트워크 상한선에서 49개 hidden이 해소된다**" | `S1_delta_vulnerability.manifest.json` → `effect_output_label="upper_bound"`, `counterfactual_effect_claim_allowed=false` · 허용 문구: `outputs/reports/plan_review_v2/09_scenario_counterfactual_runner_team_verdict.md:66` |
| "쉘터·제설로 102개가 해결된다" | "**기상항을 도시 전체에서 제거하는 상한선에서 102개 hidden이 해소된다**" | 같은 verdict 68행 |
| "S3는 효과가 없다" | "**45개 격자에서 부담은 감소했고, 임계를 넘는 hidden 해소는 0개**" | `S3_delta_vulnerability_summary.json` (`changed_cost_hex_count`=45) · 승인 문구 `plan_review_v2/10_s3_scenario_diagnostic_team_verdict.md:36,48,89` |
| "경사·기상이 서울 접근성 순위를 크게 바꿨다" | **금지 문장.** "순위는 거의 그대로지만 수준과 기준 통과 여부가 바뀐다" | `outputs/reports/plan_review_v2/08_m0_m3_effect_repair_team_verdict.md:83-88` 금지 문장 목록 |
| "전체 데이터 geometry 오류 0" | "**분석격자 레이어 geometry invalid 0**" | 최종 hex 레이어 4,551 폴리곤 한정. `hex_vulnerability_final_audit.json` → `gpkg_comparison` |
| "직선거리 400m 이내" | "**보행 네트워크 최단거리(M0) 400m 이내**" | M0는 Dijkstra 기반, 경사·기상 페널티만 제외한 값 |
| "정류장 10,887개가 깨끗하다" | "**목적지(D) 후보 10,967개 중 보행망에 연결된 10,887개**" | `outputs/reports/hex_accessibility_prelim_qa.json` |
| "계단·눈 때문에 27개 단절" | "**계단을 차단하면 27개 도달불가, 기상 조건과 무관**" | dry/rain/snow 모두 27. `m4_senior_variant_metrics.csv` |

---

# 2. 확정된 것 (검증 통과)

## 2-1. 파이프라인·데이터 손실 (슬라이드 15)

| 값 | 정의 | 출처 |
|---|---|---|
| 보행망 467,556 → 466,626 (손실 930) | edge(구간) 단위 | `outputs/reports/v2_2_execution/data_loss_ledger_v2_2.csv` |
| 930 = 고도결측 900 + 경사>100% 30 | 삭제 대상. **경사 30~100%는 삭제가 아니라 30% 상한 처리** | 같은 파일 (`row_change_type=filter_then_cost_cap`) |
| 목적지 10,967 → 10,887 (스냅 실패 80) | 버스 10,664 + 지하철 303, 보행망 100m 내 스냅 성공 기준 | `outputs/reports/hex_accessibility_prelim_qa.json` · 내역 `docs/implementation_corrections.md:158` |
| 격자 4,551 → 4,383 (손실 168) | `analysis_valid_final == True` | `outputs/reports/hex_vulnerability_final_audit.json` + parquet 재계산 |
| 168 = 스냅 실패 **167** + 도달불가 **1** | 두 집합 교집합 0건 | `data_loss_ledger_v2_2.csv` · `data/derived/hex_accessibility_prelim_M0_M3.parquet` |
| audit pass / 원자료 34개 항목 전건 ok | 공식 불일치 0, 단조성 위반 0, null 0, inf 0, hex_id unique | `hex_vulnerability_final_audit.json`, `outputs/reports/data_validation.json` |
| geometry invalid 0 (4,551 폴리곤, EPSG:5179) | 최종 hex 레이어 한정 | `hex_vulnerability_final_audit.json` → `gpkg_comparison` |

## 2-2. 환경부담 (슬라이드 19·21)

| 값 | 정의 | 출처 |
|---|---|---|
| 평균 183.89m → 212.28m (+28.39m) | 유효 4,383 hex 비가중 평균 | `outputs/reports/v2_2_execution/m0_m3_environment_burden_effect.json` + parquet |
| 중앙값 148.53 → 169.80 (+17.31m), p90 증분 66.39m | 동일 모집단 | `outputs/reports/hex_vulnerability_summary_stats_v3.csv` |
| 모델별 평균 M0 183.89 / M1 199.32 / M2 211.27 / M3 212.28 (P90 357.84/394.13/417.78/420.78) | `docs/분석_진행_정리.md` 6-1 표와 일치 | 위 json + parquet |
| +15.4364% (평균의 비율) | 정의 주의 — 3장 참조 | 같은 json `mean_cost_increase_rate` · `scripts/build_v2_2_execution_artifacts.py:431` |
| 고령가중 +15.8376% / 인구가중 +15.6737% | 가중치 `registered_senior_population` | 같은 json |
| 400m 경계 초과 167개 (그중 hidden 109), 역방향 0 | 등록인구 343,384 / 고령 70,032 | 같은 json `m0_400_to_m3_over_400_count` |
| 고정 임계 신규 취약 275개 (877→1,152, 해소 0) | 등록인구 643,039 / 고령 130,153 | 같은 json `new_environment_burden_vulnerable_count` |
| Spearman 0.994695 (Pearson 0.991933) | 접근비용 순위 상관, 취약도 점수 아님 | 같은 json |
| 순위 이동 중앙값 57 / p90 158 / p95 249 / 최대 1,146. 100계단 이상 1,258개(28.7%), 200 이상 304, 500 이상 54 | hidden 632만 보면 중앙값 100, 최대 840 | 같은 json + `.csv` `abs_rank_shift_m0_m3` |

## 2-3. 후보군 선별 (슬라이드 20·22)

| 값 | 정의 | 출처 |
|---|---|---|
| 유효 4,383 | `analysis_valid_final` | `hex_vulnerability_final_qa.json` |
| 취약 877 (임계 0.0288386 = 80분위) | `cost_m3_norm × demand_norm` 상위 20% | 같은 파일 + parquet |
| hidden 632 (877의 72.1%) | `official_400m_ok_m0 AND vulnerable_m3_final`, 조건 2개뿐 | `vulnerability.py:81-83` |
| robust-core 429 | **곱 구조 4개 정규화** 공통 (additive 제외 / 5개 전부면 338) | `hidden_vulnerable_robust_core_summary.json` · `build_v2_2_execution_artifacts.py:175-183` |
| any 1,053 / 정규화 민감 624 | 4개 variant 기준 | 같은 json |
| 4분면 고비용×고수요 185, hidden 156 (84.3%) | C4_high×D4_high, 이 칸은 100% 취약 | `quadrant_4x4_matrix.csv`, `quadrant_primary_summary.json` |
| 4×4 표 16칸 합계 = 4,383 / 877 / 632 | 세 축 모두 총계 정합 | `quadrant_4x4_matrix.csv` |

## 2-4. 노출 (슬라이드 21)

| 값 | 정의 | 출처 |
|---|---|---|
| 등록인구 1,660,653.5명 (약 166만) | hidden 632개, 면적가중 배분 | `hex_vulnerability_final_qa.json` + parquet + 행정동 요약 CSV 3중 확인 |
| 고령 342,011.7명 (약 34.2만) | 65세 이상 등록인구 | 같은 파일들 |
| 서울 대비 17.34% / 17.88% (전체 9,579,177 / 65세 이상 1,912,751) | 맥락 수치 | `registered_population_admin_qa.json` |
| 등록인구 기준(생활인구 아님), 65세 이상 | 2025년 4분기 주민등록 | `src/sl_accessibility/population/registered.py:363` |
| 참고: 877 기준은 217만/44.4만, 429 기준은 121만/25.2만 | 격자군 혼동 방지용 | parquet 재계산 |

## 2-5. 원인 유형 (슬라이드 23) — 6분류 합계 632 (100%)

| 유형 | n | % | 평균 M0 | 평균 환경증분 | 평균 수요백분위 |
|---|---:|---:|---:|---:|---:|
| 경사·기상 부담형 | 191 | 30.22 | 304.3m | 72.5m | 0.522 |
| 수요집중형 | 167 | 26.42 | 226.8m | 22.1m | 0.907 |
| 복합형 | 140 | 22.15 | 265.3m | 62.7m | 0.885 |
| 혼합형(잔여) | 86 | 13.61 | 273.6m | 25.0m | 0.621 |
| 400m 경계형 | 35 | 5.54 | 353.5m | 27.8m | 0.503 |
| 수요+경계형 | 13 | 2.06 | 357.2m | 27.4m | 0.838 |

출처: `outputs/reports/hidden_vulnerability_reason_diagnostics_qa.json`, `outputs/reports/hidden_vulnerability_reason_diagnostics.csv` (632행 직접 카운트로 재현). 임계값: 수요 75분위 ≈ 0.2643, 환경증분 75분위 ≈ 33.0m, 거리 400×0.8 = 320m.

## 2-6. 시나리오·고령 모형 (슬라이드 24·27)

| 값 | 정의 | 출처 |
|---|---|---|
| S1 632 → 583 (해소 49) | 후보 정류장 48개 전부 동시 추가, **상한 시나리오** | `scenario_counterfactual/S1_delta_vulnerability_summary.json` |
| S3 해소 0 (단 비용 감소 45개) | grade>30% edge 2,970개를 15% cap, **유일한 정식 반사실** | `S3_delta_vulnerability_summary.json` |
| S4 632 → 530 (hidden 해소 102 / 취약 해소 112) | 기상항 2개 전역 제거, **상한 시나리오** | `S4_weather_off_delta_vulnerability_summary.json` |
| 계단 차단 도달불가 27 (기상 무관) | steps edge 5,628개 제거 후 재탐색 | `m4_senior_summary.json`, `m4_senior_variant_metrics.csv` |
| 계단 의존 92 (27 포함) | 비용 비교로 유도, 경로 기하 복원 아님 | 같은 파일 |
| snow 877 → 1,143 (hidden 623 → 868) | M4-senior 비용 × 고령노출 축 | `m4_senior_variant_metrics.csv` |
| Jaccard 0.667 = 교집합 702 / 합집합 1,052, 신규 175·탈락 175, 교체율 19.95% | 노출축 교체 효과 | `m4_senior_summary.json` + 재계산 |
| 순수 임피던스 Jaccard 0.9275 = 교집합 844, 교체율 3.76% | 비용모형만 고령용으로 교체 | `m4_senior_summary.json` |
| E1(경사 cap)·E3(기상계수) 교체율 ≤0.16%, E6(k근접) 12.3%/17.7%, hidden 632~651 | 민감도 | `outputs/reports/e_sensitivity/e_sensitivity_summary.json` |
| 임계 민감도: 상위 10% → 252개/70.9만/14.8만, 20% → 632개/166.1만/34.2만, 30% → 1,040개/262.1만/53.6만 | 노출 규모의 임계 의존성 | `docs/reports/worker1_sensitivity_threshold_table.csv` |
| S1/S3/S4 세 manifest의 `frozen_baseline_threshold_hash`·`baseline_cost_hash` 전부 동일 | before/after 비교 기준 고정이 해시로 증명됨 | 각 `*_delta_vulnerability.manifest.json` |

---

# 3. 숫자 정의 대조표 (질의응답 최대 위험 구간)

## 3-1. 167 — 세 가지 뜻, 전부 실재하며 우연의 일치

| 슬라이드 | 값 | 무엇을 센 것인가 | 모집단 | 출처 |
|---|---|---|---|---|
| 15 | 167 | 격자 중심점이 보행망 노드에 100m 내 **스냅 실패** | 4,551 전체 | `hex_accessibility_prelim_qa.json` → `origin_snap_invalid_count` |
| 21 | 167 | M0 ≤ 400m 였다가 M3 > 400m로 **경계 초과** | 유효 4,383 | `v2_2_execution/m0_m3_environment_burden_effect.json` → `m0_400_to_m3_over_400_count` |
| 23 | 167 | 주원인이 **수요집중형**인 hidden 격자 | hidden 632 | `hidden_vulnerability_reason_diagnostics_qa.json` → `primary_reason_counts.high_demand` |

- **전사 오류가 아님이 확인됨.** 세 값 모두 서로 다른 파일에서 독립 확인했고, (15)와 (21)은 parquet에서 직접 재계산해 각각 167임을 검증했습니다.
- **결정적 반증 근거:** (21)과 (23)은 교집합조차 167이 아닙니다. 400m를 넘어선 167개 중 hidden에 속하는 것은 **109개**뿐이고(`m0_400_to_m3_over_400_hidden_count`=109, CSV 632행에서 재현), hidden 632개는 정의상 전부 M0 ≤ 400m이므로 두 지표는 모집단부터 다릅니다.
- **발표 규칙:** 세 개를 같은 화면에 두지 말 것. 매번 분모를 붙여 읽을 것 — "4,551개 중 보행망에 붙지 않은 167개", "4,383개 중 400m를 넘어선 167개", "632개 중 수요집중형 167개".

## 3-2. 877 — 네 가지 뜻, 개수가 같은 것은 필연

| 라벨 | 정의 | 출처 |
|---|---|---|
| (a) M3 취약 877 | M3 비용 × 수요지수 상위 20% | `hex_vulnerability_final_qa.json` → `vulnerable_m3_final_count` |
| (b) M0 frozen 취약 877 | M0 비용 frozen baseline 상위 20% | `m0_m3_environment_burden_effect.json` → `m0_frozen_vulnerable_count` |
| (c) M4-senior 취약 877 | M4 고령 임피던스 비용 × 고령인구 노출 (rain × steps_penalty 기준변형) | `m4_senior_variant_metrics.csv` → `senior_vulnerable_count` |
| (d) M3비용 × 고령노출 877 | 비용은 M3, 노출축만 고령인구 | `m4_senior_summary.json` → `m3cost_senior_exposure_vulnerable_count` |

- 넷 다 "유효 4,383의 상위 20% 분위 컷"에서 나오므로 **개수가 같은 것은 구조적 필연이고, 구성원이 같은 것은 아닙니다.**
- 집합 관계: (a)∩(c) = 702 (Jaccard 0.667), (c)∩(d) = 844 (Jaccard 0.9275). (b)는 (a)와 다른 집합입니다.
- **슬라이드 24 필수 문장:** "S1·S4의 632는 M3 비용 × 수요지수 축, snow의 877은 M4-senior 비용 × 고령인구 노출 축입니다. 개수는 같지만 같은 집합이 아닙니다." M4-senior는 `frozen_baseline_threshold_hash`부터 다릅니다(S1/S3/S4는 `b055dd1c…`, M4는 `343737d0…`).

## 3-3. 632 / 429 — 같은 값, 다른 역할

| 맥락 | 의미 | 출처 |
|---|---|---|
| 슬라이드 20 | **선별 결과** — 877 중 M0 400m 이내인 632 (나머지 245는 M0로도 이미 400m 초과) | `hex_vulnerability_final_qa.json` |
| 슬라이드 24 | **시나리오 기준선** — S1/S4의 before 값 | `S1/S4_delta_vulnerability_summary.json` |
| 429 | baseline hidden 632의 **부분집합**, 곱 구조 4개 정규화 공통 | `hidden_vulnerable_robust_core_summary.json` |

## 3-4. 나머지 혼동 유발 쌍

| 쌍 | 관계 | 주의점 |
|---|---|---|
| 27 / 92 | **포함 관계**. 92 = 계단 경로 의존 격자 = 우회 비용 증가 65 + 우회 불가 27 | 별개 숫자로 말하면 안 됨. 27은 기상과 무관(키 이름 `barrier_snow_unreachable_hex`가 오해 유발) |
| S4의 102 / 112 | hidden 해소 102, 취약 해소 112 | 슬라이드 24는 hidden 축 → **102** |
| S1의 49 / 49 | hidden 해소 49, 취약 해소 49 — 우연히 같음 | 비용이 실제로 바뀐 격자는 55개 |
| 복합형 140 / 혼합형 86 | 의미가 **정반대**. 140 = 수요·환경 둘 다 높음, 86 = 셋 다 낮은 잔여군 | 한글 라벨이 사실상 동의어. 86을 35·13과 묶어 "혼합·경계 134"로 처리 권장 |
| 134 | = 혼합 86 + 400m 경계 35 + 수요+경계 13 | 접기 로직은 `scripts/build_qgis_midterm_project.py:321-325`에 이미 구현 |
| 930 / 168 | 930 = 900 + 30 (edge), 168 = 167 + 1 (격자). 단위가 다름 | ledger에 4,551→4,383이 두 줄(`accessibility_m0_m3`, `analysis_valid_final`) 있는데 같은 168 손실의 다른 단계 기록 |
| 15.4% / 15.16% / 11.19% | 15.4% = 평균의 비율, 15.16% = 격자별 증가율의 단순평균(n=3,933, M0=0인 450개 제외), 11.19% = 그 중앙값 | "평균 접근비용이 15.4% 증가"는 정확, "평균적인 격자가 15.4% 증가"는 부정확 |
| 교체율 20% / 33% | 20% = \|A−B\|/\|A\| (집합 기준, `build_e_sensitivity_runner.py:73`), 33% = 1−Jaccard (합집합 기준, `build_v2_2_execution_artifacts.py:134`) | **저장소 안에 같은 이름의 지표가 두 정의로 존재.** E6의 12.3%/17.7%(전자)와 정규화 실험의 60.4%/68.6%(후자)를 같은 표에 넣지 말 것 |
| 624 / 203 | 624 = 정규화 민감 격자 수, 203 = 그중 행정동 라벨이 붙은 수 | 1-2의 항목 I 참조 |
| 654 / 796 | `hidden_vulnerable_robust_core.csv`의 additive_minmax_hidden True는 654개로 보이지만 실제 additive hidden 총수는 796 | 해당 CSV가 4-variant 합집합 1,053행으로 이미 필터된 파일이기 때문. 미리 밝힐 것 |
| S3·S4 (두 레지스트리) | 반사실 레지스트리: S3=경사 cap15, S4=기상항 제거. v2_2 레지스트리: S3=효과범주 진단표, S4=기상대응 Top20 | 슬라이드 24의 S3/S4는 **`scenario_counterfactual/` 쪽**임을 특정해서 말할 것 |

---

# 4. 질의응답 대비

## 4-1. "Spearman 0.995면 순위가 그대로인데, 경사·기상을 넣어서 얻은 게 무엇입니까?" (최우선)

**답변 (그대로 읽을 수 있는 완성 문장):**

> "먼저 그 숫자의 정체를 정확히 말씀드리면, 0.995는 취약도 점수가 아니라 접근비용의 순위 상관이고, 저희 주장도 순위 재배열이 아닙니다. 네 가지로 답하겠습니다.
>
> 첫째, 순위 상관은 수준 이동에 둔감합니다. 서울 전체 평균 접근비용은 183.9m에서 212.3m로 28.4m, 15.4% 증가했고, 고령인구로 가중하면 15.8%입니다. 상위 10% 부담 격자는 66.4m가 늘었습니다.
>
> 둘째, 상관이 0.995라고 개별 격자가 안 움직인 것이 아닙니다. 4,383개 중 1,258개, 28.7%가 순위 100계단 이상 이동했고, 304개는 200계단 이상, 최대 이동은 1,146계단입니다. 저희가 고른 hidden 632개만 보면 순위 이동 중앙값이 100계단입니다.
>
> 셋째, 행정 기준의 통과 여부가 실제로 바뀝니다. 순수 거리 기준으로는 400m 이내였다가 경사·기상을 반영하면 400m를 넘어가는 격자가 167개이고, 그 안의 등록인구가 34.3만 명, 고령인구가 7.0만 명입니다. 반대 방향, 즉 400m 밖에 있다가 안으로 들어온 격자는 0개입니다.
>
> 넷째, M0로 정규화와 임계를 고정한 상태에서 비용만 M3로 바꾸면 취약 격자가 877개에서 1,152개로 늘어납니다. 신규 취약 275개, 등록인구 64.3만 명, 고령 13.0만 명이고 해소된 격자는 0개입니다.
>
> 정리하면, 경사와 기상은 서울의 접근성 순서를 바꾸는 요인이 아니라 **기준선을 넘기는 요인**입니다. 순위 재정렬은 저희가 주장하지 않습니다."

**근거 파일:** 모두 `outputs/reports/v2_2_execution/m0_m3_environment_burden_effect.json` (`mean_cost_increase_rate`, `senior_weighted_cost_increase_rate`, `abs_rank_shift_ge_100_count`=1258, `abs_rank_shift_p90`=158, `m0_400_to_m3_over_400_count`=167, `new_environment_burden_vulnerable_count`=275) 및 `outputs/reports/hex_vulnerability_summary_stats_v3.csv`. 팀 판정문 `outputs/reports/plan_review_v2/08_m0_m3_effect_repair_team_verdict.md:79-88`이 이 2층 분리(순위 재정렬 주장 금지 / 환경부담 주장 허용)를 이미 확정해 두었습니다.

**보너스 카드 (여유가 있을 때만):** "저희가 고른 hidden 632개 그룹의 환경 증분 평균은 47.1m로 서울 전체 평균 28.4m의 1.66배입니다. 다만 이 그룹은 애초에 환경 부담이 큰 격자로 선별된 부분집합이라는 점을 함께 말씀드립니다." (`hidden_vulnerability_reason_diagnostics.csv` 632행 평균)

**재반문 A — "그럼 M0로만 계산해도 632개 나오는 것 아닙니까?"**
> "맞습니다. 거리만으로 취약도를 계산해도 hidden은 634개가 나오고, 632개와 574개가 겹칩니다. 그래서 저희는 632라는 개수 자체를 경사·기상의 효과라고 주장하지 않습니다. 경사·기상이 추가한 것은 방금 말씀드린 절대 수준 +28.4m, 400m 경계를 넘긴 167개, 고정 임계 기준 신규 취약 275개, 그리고 hidden 명단 안의 58개 교체입니다. 632개가 나오는 근본 이유는 현행 400m 기준이 거리만 보고 수요를 보지 않기 때문입니다."

**재반문 B — "순위가 안 바뀌면 정책적으로 의미가 없지 않습니까?"**
> "정책 기준은 순위가 아니라 400m라는 절대선으로 운영됩니다. 순위가 그대로여도 절대선을 넘는 격자가 167개 생기면 행정적 판정이 바뀝니다. 그것이 이 모형의 실무적 기여입니다."

## 4-2. 그 외 예상 질문

| 질문 | 답변 요지 | 근거 |
|---|---|---|
| "877은 4,383의 20%인데 876.6 아닙니까?" | "80분위 임계 이상(>=) 카운트라 877개, 비율로는 20.009%입니다. 곱셈으로 구한 값이 아닙니다." | `vulnerability.py:79-81` |
| "강건성 표에는 정규화가 5개인데 429는 왜 4개 교집합입니까?" | "429는 곱 구조 4개 정규화의 공통 집합입니다. 가산형(additive)은 취약도 정의 구조 자체가 달라 제외했고, 5개 전부를 넣으면 338개가 됩니다. 두 숫자를 함께 제시하겠습니다." **주의: 제외 근거는 코드·문서에 명시돼 있지 않으므로 "구조가 다르다"는 논리로만 답할 것** | `build_v2_2_execution_artifacts.py:175-183` |
| "4개 정규화를 통과했다니 검증이 촘촘하군요" | "정확히 말씀드리면 429는 사실상 log1p 하나가 결정합니다. baseline∩log1p가 429이고 winsorize·rank는 추가로 깎지 못합니다. log1p가 가장 가혹한 검사입니다." (먼저 밝히는 쪽이 신뢰가 올라감) | `normalization_combination_threshold_robustness.json` (`shared_hidden_with_baseline`=429) |
| "복합형과 혼합형의 차이가 뭡니까?" | "복합형 140개는 수요와 환경 압력이 둘 다 높은 격자이고, 혼합형 86개는 세 압력 어디에도 걸리지 않은 잔여군입니다. 이름이 혼동을 주어 발표에서는 86·35·13을 묶어 혼합·경계 134개로 표시했습니다." | `vulnerability.py:174` |
| "S3가 0이면 경사 개선은 무의미합니까?" | "hidden 해소가 0이라는 뜻이고, 45개 격자에서는 부담이 실제로 감소했습니다. 다만 비용 생성 단계에서 이미 경사가 30%로 상한 처리되므로, S3는 급경사 전반이 아니라 모형상 30%로 상한된 구간을 15%로 낮추는 좁은 개입입니다." | `S3_delta_vulnerability_summary.json`, `plan_review_v2/10_...:89` |
| "S4는 기상 대응 Top20 아니었습니까?" | "레지스트리가 두 개이고 ID가 겹칩니다. 슬라이드 24의 S4는 반사실 레지스트리의 기상항 제거 상한선(632→530)이고, 기상 대응 Top20은 v2_2 실행 레지스트리 쪽 별개 산출물로 슬라이드 27의 정책 후보로 연결됩니다." | `scenario_counterfactual_registry.csv` vs `v2_2_execution/scenario_effect_registry_v2_2.csv` |
| "166만 명은 상위 20%라는 임계 선택에 달린 값 아닙니까?" | "맞습니다. 상위 10%면 252개 격자·70.9만 명, 20%면 632개·166.1만 명, 30%면 1,040개·262.1만 명입니다. 다만 경사·기상 파라미터를 0.7~1.3배로 흔들면 hidden은 632~633개, 인구는 166.1만~166.7만 명으로 거의 변하지 않습니다." | `docs/reports/worker1_sensitivity_threshold_table.csv`, `..._parameter_table.csv` |
| "등록인구입니까 생활인구입니까?" | "등록인구입니다. 같은 632개 격자의 생활인구 평균 합은 145.9만 명, 생활 고령인구는 31.0만 명으로 다릅니다." | `data/derived/hex_vulnerability_final.parquet` (`registered_*` vs `living_*`) |
| "고령자 모형으로 바꾸면 결과가 크게 달라집니까?" | "비용모형만 고령용으로 바꾸면 후보의 3.8%만 바뀌고, 노출축을 고령인구로 바꾸면 20%가 바뀝니다. 즉 결과를 움직이는 레버는 '어떻게 걷느냐'가 아니라 '누구를 보느냐'입니다." (두 숫자 모두 같은 척도) | `m4_senior_summary.json` (Jaccard 0.9275 / 0.6673) |
| "고령 임피던스만으로 새 취약지를 찾았다는 뜻입니까?" | "아닙니다. 산출물의 판정문도 그 주장을 금지합니다. M4-senior는 관측 보행자료로 보정된 모형이 아니라 프로필 시나리오입니다." | `m4_senior_summary.json` → `verdict`, `claim_boundary` |
| "930개를 뺐다면 급경사 구간을 다 제외한 겁니까?" | "아닙니다. 삭제는 고도 결측 900개와 경사 100% 초과 30개뿐이고, 경사 30~100% 구간은 삭제가 아니라 30%로 상한 처리했습니다." | `data_loss_ledger_v2_2.csv` (`30-100% grades are capped, not dropped`) |
| "손실 원장에 정류장 줄이 왜 없습니까?" | "10,967→10,887은 원장이 아니라 접근성 QA 파일에 기록되어 있습니다." | `hex_accessibility_prelim_qa.json` |
| "원장에 4,551→4,383이 왜 두 번 나옵니까?" | "같은 168개 손실을 접근성 계산 단계와 최종 점수 산정 유니버스 필터 단계에 각각 기록한 것입니다." | `data_loss_ledger_v2_2.csv` |
| "robust-core CSV의 additive가 654개인데 표에는 796개입니다" | "그 CSV는 곱 구조 4개 variant의 합집합 1,053행만 담은 파일이라 additive 전체가 들어 있지 않습니다. additive 단독 hidden 총수는 796개입니다." | `hidden_vulnerable_robust_core.csv`, 강건성 표 |
| "before/after 비교 기준이 도중에 바뀌지 않았습니까?" | "S1·S3·S4 세 시나리오의 매니페스트에 기록된 frozen threshold 해시와 baseline cost 해시가 전부 동일합니다. 기준 고정이 해시로 증명됩니다. 단 M4-senior는 다른 기준선입니다." | 각 `*_delta_vulnerability.manifest.json` |
| "이 원인 분류는 인과입니까?" | "진단 범주이며 인과 증명이 아닙니다. 세 압력의 임계도 절대 기준이 아니라 유효 격자의 75분위입니다." | `fig07` 각주 "Primary reason is a diagnostic category, not causal proof." |

---

# 5. 여전히 확인 안 된 것

| 항목 | 상태 | 영향 / 대응 |
|---|---|---|
| **교체율(replacement rate) 지표** | **not_found.** `m4_senior_variant_metrics.csv` 16개 열이 전부 count·spearman·jaccard이며 교체율 열 0개. `m4_senior_summary.json`의 `acceptance_check.validity_metrics_reported`에는 "replacement_vs_reference"를 보고했다고 선언되어 있으나(`build_m4_senior_runner.py:471`) 실제 계산된 것은 Jaccard뿐 — 선언과 실물 불일치 | 20% 교체율은 **재계산으로만** 얻은 값입니다. 교집합 702 / 합집합 1,052 / 신규 175 / 탈락 175 역시 어느 파일에도 저장돼 있지 않으므로, 이 숫자들을 슬라이드 노트에 미리 적어 두십시오. "1/3 교체" 오독이 발생한 구조적 원인입니다 |
| **QGIS 지도 산출물** | **not_found.** 저장소 루트에 `qgis/` 디렉터리 자체가 없고 `.gpkg` 파일 0개. `m0_m3_environment_burden_shift.gpkg` 등은 `plan_review_v2/08_...:26`, `v2_2_execution_summary.md:56,58,62`에 참조만 존재 | 슬라이드 22에 지도를 넣으려면 `scripts/build_v2_2_execution_artifacts.py` 재실행 필요. 대안으로 `outputs/figures/`의 `01_cost_functions.png`, `04_vulnerability_results.png`, `outputs/figures/midterm/fig07_reason_diagnostics.png`, `fig08_policy_scenario_diagnostics.png`, `outputs/qgis_midterm_maps/S8_reason_diagnostics.png`은 존재 확인됨 |
| **감사 JSON이 참조하는 GPKG 실물** | 미확인. `hex_vulnerability_final_audit.json`의 `gpkg_comparison`(geometry invalid 0, 4,551 폴리곤, parquet 일치)은 **JSON에 기록된 값만** 확인했고, `qgis/out_hex_vulnerability_final.gpkg` 파일 자체는 위 항목대로 저장소에서 발견되지 않음 | "geometry invalid 0"은 감사 기록 기준이라고 말할 것. 파일 실물을 슬라이드에서 시연할 계획이면 사전 재생성 필요 |
| **원자료 CSV** | 미확인. `data/walking_network_edges_with_slope_google.csv` 등 원자료가 라이선스상 레포에 없어 467,556→466,626을 직접 재계산 불가 | 문서·스크립트 5곳(`docs/implementation_corrections.md:196-199`, `docs/setup/data_acquisition.md:81`, `docs/qgis/04_network_slope_outlier_qc.md:60-62`, `docs/DATA_LICENSES.md:11`, `scripts/build_midterm_visuals.py:161`) 일치로 대체 확인. 질문 시 "원자료는 재배포 제한이라 산출물과 문서 교차확인으로 검증했다"고 답할 것 |
| **robust-core에서 additive_minmax를 제외한 근거** | 미확인. 코드는 `flag_cols`에서 단순히 빼놓았을 뿐(`build_v2_2_execution_artifacts.py:175-183`)이고, 제외 사유가 코드 주석·문서 어디에도 명시돼 있지 않음 | "곱 구조와 가산 구조는 취약도 정의가 다른 모형"이라는 논리로 답하되, **문서에 근거가 있다고 말하지 말 것**. 429와 338을 둘 다 제시하는 편이 안전 |
| **4분면 수치의 다른 버전** | 미확인. `plan_review_v2/02_critical_sensitivity_recheck_v2_2.md`에 "high-cost + high-demand among vulnerable 98/877 = 11.2%"라는 다른 숫자가 있으나 컷 기준이 달라 보이고 관계를 규명하지 못함 | 185/156과 98/11.2%를 **같이 쓰지 말 것**. 슬라이드에는 `quadrant_primary_summary.json` 기준(185/156)만 사용 |
| **검증 환경 변경** | `/Users/vista/miniconda3/envs/project`에 `pyarrow 25.0.0`을 설치했습니다(원래 없었음). parquet 직접 재계산을 위한 조치입니다 | 원상복구가 필요하면 `/Users/vista/miniconda3/envs/project/bin/python -m pip uninstall pyarrow` |